import { ResizeOneColDirective } from './resize-one-col.directive';

describe('ResizeOneColDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeOneColDirective();
    expect(directive).toBeTruthy();
  });
});
