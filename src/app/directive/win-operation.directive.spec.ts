import { WinOperationDirective } from './win-operation.directive';

describe('WinOperationDirective', () => {
  it('should create an instance', () => {
    const directive = new WinOperationDirective();
    expect(directive).toBeTruthy();
  });
});
